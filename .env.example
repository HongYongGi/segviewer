# === 필수 설정 ===
# nnUNet_results 디렉토리 경로 (학습된 모델 weights가 있는 곳)
NNUNET_RESULTS_PATH=/path/to/your/nnUNet_results

# === 선택 설정 (기본값 있음) ===
UPLOAD_DIR=./uploads
RESULTS_DIR=./results
MAX_UPLOAD_SIZE_MB=500
MAX_CACHED_MODELS=2
GPU_DEVICE_INDEX=0
LOG_LEVEL=INFO
